fn main() {
    println!("arc — open-source deterministic power flow kernel (v0.1-garage)");
}

#[cfg(test)]
mod tests {
    #[test]
    fn cli_scaffold_verification() {
        assert_eq!(1, 1);
    }
}
