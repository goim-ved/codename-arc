[package]
name = "arc-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Core data models, admittance matrix builders, and power flow solvers for arc"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
